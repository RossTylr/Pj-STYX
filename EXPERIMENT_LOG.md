# EXPERIMENT_LOG.md — STYX

Append-only. One row per slice when its gate passes (BUILD_MVP.md). The
AEGIS→threshold lead-time is logged every time `synth/`, `forecast/`, or `risk/`
changes (Hard Rule 4).

| Slice | Gate | Result | Lead-time (min) | Date |
|-------|------|--------|-----------------|------|
| S0 — scaffold | none | repo skeleton runs; `pytest` collects; `streamlit run app/app.py` launches | — | 2026-06-06 |
| S1 — synth (F5) | **G1 ✅** | determinism (seed=42 ×2 identical); dissociable silent window; genuine RR–SpO₂ decoupling; ≥12-patient cohort, outcome-from-history AUC=1.000; `tests/test_g1.py` 9/9 green; `notebooks/01_synthetic_fidelity` renders 3 panels | **160** (decoupling onset → first sustained breach, seed=42) | 2026-06-06 |
| S1-refine — outcome realism | **G1 ✅ (re-check)** | outcome now *stochastic* (`P=sigmoid(5·(frailty−½))`, sampled); AUC scored on noisy observed `comorbidity_index` (not frailty); learnability is a **band** [0.60, 0.90] not a floor; cohort default 50. **AUC seed=42 = 0.782** (was 1.000); mix 21 ESCALATED / 29 RECOVERED; determinism intact; silent window + lead unchanged; `pytest` 9/9; `ruff` clean | **160** (unchanged; patient-0 stream bit-identical, verified n=12≡n=50) | 2026-06-06 |
| S2 — state + viz (F1) | **G2 ✅ (constructed)** | 2-D embedding of `VITALS` w/ named axes (oxygenation, effort), learned basin/attractor, pure `trajectory_figure`. PCA **fell back** to constructed: PC0 alone tracks *both* anti-correlated constructs (oxy r=−0.97 / effort r=+0.98), PC1 dominated by labs_proxy (loading 0.94, r≈0.10) → axis-1 illegible. Constructed axes: **oxygenation r=1.000, effort r=0.996** (both ≥0.60). Silent case drifts basin→attractor (**+1.378**). DET-1 identical coords; `tests/test_g2.py` 3/3; G1 re-run 6/6 (synth untouched); `ruff` clean; `notebooks/02_state_space` renders legibility panel + 3 trajectories | **160** (G1 re-check, unchanged — S2 reads cohort only) | 2026-06-09 |
| S1-enrich — archetypes + silent hypoxia | **G1 ✅ (re-check)** | deterioration **archetypes** (SILENT_HYPOXIA / COMPENSATED / COUPLED + STABLE) dissociate oxygenation from effort; patient 0 re-scripted as **silent hypoxia** (SpO₂ falls, RR/HR flat). `has_silent_window` **generalised** (locked decision): in-range + SpO₂-slope<0 + decoupling present; **dropped the RR-rising requirement** (silent hypoxia has effort flat). Labs (`labs_proxy`) given a later, independent onset (`T_LABS_MIN=660`) → semi-independent signal. Onset moved 600→**540** for lead headroom. **AUC seed=42 = 0.765** (in band, no creep); mix 21 ESCALATED / 29 RECOVERED (silent 6 / compensated 4 / coupled 11). DET-1 bit-identical; `tests/test_g1.py` 6/6; `ruff` clean | **200** (seed=42); **60-seed sweep min 130 / median 200 / max 245** — headroom restored (was min 90) | 2026-06-09 |
| S2-refit — state on enriched cohort | **G2 ✅ (constructed)** | re-fit `fit_embedding` on the enriched cohort; fork **re-run, PCA still falls back** (PC0 still absorbs both: oxy −0.82 / effort +0.89; PC1 a weak mix 0.34/0.45; expl-var 69/14/10) — **not forced** (constructed valid). Constructed axes **oxy r=1.000, effort r=0.995**. *Payoff:* the named plane is now genuinely 2-D — archetype now-centroids separate **off-diagonal**: silent (oxy −3.4, **effort −0.5**), compensated (oxy −0.8, **effort +7.8**), coupled (−3.5, +4.3), stable basin (+0.7, −0.7). Silent case drift **+0.69** (>0). `tests/test_g2.py` 3/3; full suite 12/12; `ruff` clean; notebooks 01 (archetype panel) + 02 (separation scatter + 3 distinct trajectories) render | **200** (G1 re-check; S2 reads cohort only) | 2026-06-09 |
| S3 — forecast, risk, AEGIS (F2, F4, F7) | **G3 ✅** | anticipation dissociated on patient 0, **in order and stable at cadence**. *Phase 0:* `learn_basins` now learns **one attractor per escalating archetype** (silent_hypoxia / compensated / coupled — no clustering RNG, DET-1 by construction); `trajectory_drift` measures travel toward the **nearest** mode → silent-case drift **+0.69 → +1.10** (selects the oxygenation-led mode); G2 re-run 3/3. *F2:* deterministic least-squares trend + **split-conformal** bands (fixed non-index calibration, empirical quantile, clipped [0,1], widens 0.09→0.24); "fires" = cone upper edge reaches threshold for 3 **consecutive** re-scores (sustain rejects the early transient that else fires before AEGIS). *F4:* continuous waterline `risk = 0.5·proximity + 0.5·exceedance` — proximity rises in the silent window (max 0.26 < 0.5) but the absolute NEWS2-style exceedance term gates the crossing, so 0.5 is crossed **only at breach** (fires last). *F7 AEGIS:* trend-smoothed **2-D state-position** departure from personal baseline (max-axis, generalises beyond hypoxia — fires on compensated too), sustained K=3σ. **Firing order (at-cadence 15 min): AEGIS 705 → forecast 750 → threshold 915**; raw 700 → 730 → 910. `tests/test_g3.py` 4/4 (determinism, order, lead≥floor, cadence preserves); G1 6/6 (synth untouched) + G2 3/3 + full suite 16/16; `ruff` clean; `notebooks/03_anticipation_lead` renders 3 panels. **⚠ labs_proxy is dead-weight in S3** (constructed embedding gives it 0 loading → no proximity; max 1.057 → never the worst-vital exceedance) — flagged per plan; SIG-1 slot to revisit at deployment, out of S3 scope (Hard Rule 4 forbids touching synth here) | **210** at-cadence (= raw; cadence preserves, floor `AEGIS_LEAD_FLOOR_MIN`=180). **Adequacy: clinically meaningful (~3.5 h)**, not thin | 2026-06-09 |
| S4 — Theograph, CALLIOPE, ghost + patient page (F3, F9, F8) | **G4 ✅** | the integrated single-patient demo. *F3 Theograph:* `styx/theograph` materialises the per-channel event **counts** (`Patient.theograph`) into a dated, **recency-biased** timeline, seeded from `pid` (DET-1, **no `synth` touch** → G1 intact); dual-scale pure builders (lifelong **ribbon** in years + recent-days **detail strip**) + events threaded onto the trajectory (additive `events=` arg). *F8 CALLIOPE:* `styx/rationale` strict template over **read-only** risk accessors — `proximity_components` splits the proximity dot-product **exactly additively** per named axis (Σ = `_proximity`), plus `exceedance_per_vital`, `decoupling_drop`, `aegis_axis_departures`; headline names the **top-1 additive risk driver** (unambiguous, no tie-wobble), expand adds the AEGIS context; **closed 5-term vocabulary** `[oxygenation proximity, effort proximity, per-vital exceedance, breathing–oxygen decoupling, departure direction]` — never free text, never out-of-set. *F9 ghost:* the **stale-forecast** ghost = `project()` re-run at the AEGIS fire-index, faint dashed overlay on the realised path (additive `ghost=` arg; counterfactual ghost deferred — would touch `synth`). *Page:* `app/pages/01_patient.py` is **logic-free** (LYR-1) — all per-frame state from `styx/frame.py` (`PatientContext` built once + cheap `patient_frame` per scrub); map + waterline + cone + ghost + dual Theograph + raw-vitals + CALLIOPE + **SENTINEL** confidence (from conformal band width) under a scrub clock; `02_ward` stub for nav. **`tests/test_g4.py` 5/5** (determinism, vocab-closed, additive completeness, top-1 faithfulness, template-only). **Top-1 faithfulness 137/138 = 0.993** (floor 0.90) against the **archetype** oracle (the generating mechanism, blind to the attribution): coupled 93/93, silent_hypoxia 44/44, compensated 0/1 (its lone evaluable pre-breach point — compensated breaches fast, so almost no silent window; a near-tie at onset). Risk accessors touched `styx/risk` → **re-ran G1 6/6 + G2 3/3 + G3 4/4, full suite 21/21**; `ruff` clean; notebooks `04_rationale_faithfulness` + `05_walkthrough` (started) render; AppTest runs all pages no-exception | **210** (G3 re-check — risk accessors are read-only, score unchanged) | 2026-06-09 |
| S4.5 — patient-page polish (correctness · honesty · explainer · re-layout) | **G4 ✅ (6/6)** | a *rendering*-only pass (risk math untouched → **G1/G3 unaffected**), closing the post-breach landmine the 1425-min capture exposed. *Correctness:* `explain()` is now **regime-aware** — `Rationale` gains `regime` ("silent"|"crossed") + `additive` (top_k sums to risk). Post-breach the proximity overshoots the attractor and clips, so `additive` goes False and the renderer **suppresses the contributor panel** (it was showing 1.30 against a 1.00 risk); the headline verb switches to "**threshold crossed — in the {mode} mode**" (never "approaching" a mode already crashed through); the departure **σ clamps to words** above 8σ ("far beyond personal baseline", not "26.7σ"). *Default frame:* the clock lands on the **silent window** (`default_idx` ≈ nearest re-score to the forecast fire → risk 0.26, "approaching" — the money shot a threshold alarm can't show), not the post-crash end. *Honesty:* hid the Streamlit **Deploy** button (`.streamlit/config.toml` `[client] toolbarMode="minimal"`) that contradicted the "not a live deployment" banner. *Explainer layer:* new **pure-data** `styx/explain.py` (`Explainer` what/how/why per component; precedent — `styx/viz` already holds streamlit-free presentation) consumed by ⓘ `st.popover`s + an "Explain this page" toggle; `tests/test_explainer.py` 3/3 — **completeness** (registry == 9 COMPONENTS) + **honesty lint** (no "predicts the patient"/"diagnoses"/"learns the patient"/"real-time"; synthetic/replay/constructed anchors present). *Re-layout:* hero-first — slim banner → human title (enum demoted to a tag) → **status row** (Risk+verb · SENTINEL · **AEGIS lead promoted** "3.5 h before threshold") → **large trajectory hero** → waterline‖cone (ghost toggle moved under) → CALLIOPE (headline always; contributors collapsed, shown only when `additive`) → Theograph + raw-vitals collapsed. *Scrub:* signal **ticks** (AEGIS/forecast/breach, from `ctx.ticks`) + **jump buttons** + **step ▶** (no auto-play timer); all positions computed in `styx/frame.py` (LYR-1 — page computes nothing). **Verified via AppTest** at the breach boundary: final frame → "threshold crossed (risk 1.00)", contributor expander **absent**, σ shows "far beyond". Faithfulness **137/138 = 0.993** unchanged; full suite **25/25** (G1 6 · G2 3 · G3 4 · G4 6 · explainer 3 + smoke); `ruff` clean; notebooks 04/05 updated to v2 fields and render | **210** (unchanged — no risk-math touch; G1/G3 hold by construction) | 2026-06-09 |

| S5 — ward board + ECHO (F6, F10) | **Milestone ✅** | the 10-feature MVP runs end-to-end on the pre-baked scenario. *Surface-only* — new `styx/cohort` organises existing per-patient outputs and changes no maths, so **G1–G4 hold untouched** (re-run below). *F6 ward triage:* `build_cohort_context` fits embedding/basins/conformal-band **once** (vs `frame.build_context`'s per-patient refit — avoids O(N²)), then a cheap per-clock `ward_frame` → frozen `WardRow`s. **Time-to-escalation is banded** off the cone (upper-edge → point crossing = (soonest, central); `eta_confident` False when only the band edge crosses) — never a hard minute (UQ-1). Explicit **trichotomy** {escalated / escalating / no-forecast} as the primary sort bucket (no NaN sort); stable sort, pid tiebreak (DET-1). Flags: **silent-but-rising** (AEGIS fired by now ∧ risk<thr), **quietest** (lowest absolute exceedance), **new-low-history** (bottom-quartile Theograph density ∧ at-risk). *F10 ECHO:* deterministic **shape-aware** k-NN (k=3, self-excluded, pid tiebreak) over a fixed-length (32) index-resample of the 2-D state path — grounding, not prediction; archetype-share **63/63 = 1.000**. *Pages:* `02_ward.py` promoted (triage board + watchlist + **focus mode**), thin client on the **shared `scrub_pos` clock**; click→drill carries pid+t into `01_patient.py` (pre-set `patient_pick`+`scrub_pid` so its reset branch is skipped — verified). 3 new explainer entries (ward_board/watchlist/echo) + COMPONENTS, `test_explainer` 3/3 still green. *Milestone:* `tests/test_milestone.py` **4/4** (AppTest: ward+patient render no-exception, drill carries the clock, 10-feature presence; compute-side: patient-0 on the watchlist at the silent window, climbs rank 7→0 escalating→escalated under scrub, AEGIS<forecast<threshold ordered). **Labs decision — dropped** `labs_proxy` for a clean tight set (config `VITALS`/`NORMAL_RANGES` + `test_smoke` len 5→4 only; `scenario.py`/`cohort.py` untouched so the RNG draw order and the trailing comorbidity draw are preserved → bit-identical active streams). **Provably inert: G1/G2/G3 re-run identical** — `emb.mode` stays `constructed` (oxy r=1.000, effort r=0.995), decoupling lead 200, AUC 0.765, lead 210; the labs fork-flip contingency did **not** trigger. `notebooks/05_methods_story` renders: embedding legibility, **conformal coverage empirical mean 0.914 / min 0.900 (nominal 0.90)**, forecast reliability, lead distribution (cohort median 270, min 195), ECHO sanity. Full suite **29/29** (G1 6 · G2 3 · G3 4 · G4 6 · explainer 3 · smoke 3 · milestone 4); `ruff` clean (styx/app/tests/notebooks); all pages via AppTest no-exception. **Tag this commit as the 10-feature MVP.** | **210** (Milestone re-check — surface-only + inert labs drop; G1/G3 bit-identical) | 2026-06-09 |

| S5.5 — pre-flight (timeline · cohort-signal · baseline lock) | **Baseline ✅** | pre-R1 pre-flight, **no model maths touched** → G1–G4 hold by construction and the re-run proves it. *P1 timeline:* new `styx/timeline.py` `episode_timeline(ctx)` **assembles** the existing fire-points — AEGIS/forecast/breach read straight off `PatientContext` (`ctx.aegis_idx`/`ctx.ticks`, no re-call) + the banded ETA via the **shared** `eta_band` (promoted from `ranking._eta_band`; ward board + timeline now band identically). Pure `styx/viz/timeline.py::timeline_figure` strip (ETA drawn as a **band/open-ended ≥**, never a point — no false precision); wired logic-free above the patient-page waterline; 4 lay labels added to `styx/explain.py` (honesty-linted). `tests/test_timeline.py` 4/4 incl. **single-source** (`tl.aegis==ctx.aegis_idx==aegis_fire_index(...)`, `tl.{forecast,breach}==ctx.ticks[...]`, ETA==`eta_band(project(@default_idx))`) — the timeline cannot drift from its sources. *P2 cohort-signal* (`notebooks/06_cohort_signal.py`, read-only, ships no claim) — **the four numbers**: (1) **history marginal value `combined − telemetry = +0.000`** (telemetry saturates AUC=1.000 in-sample at the silent window; history-only 0.765) → **R1 must be descriptive** (history *explains* baseline/basin/hazard), not a prediction claim; (2) watchlist **precision 1.000 / recall 1.000**, median AEGIS→threshold lead **270 min** (range 195–330) — perfect *construct validity* (recovers the 21 scripted escalators), not ward performance; (3) **quietest = pid 2 is stable, NOT on the watchlist** (overlap empty — the "calmest deteriorating" beat belongs to patient 0, who reads in-range yet is silent-but-rising, not the single calmest); (4) **new-low-history = pid 39** (Theograph density 15 < q25 18.2, compensated, escalated, AEGIS fired pre-`di`) — the live-signal catch a history prior would miss (R1 residual). *P3 baseline lock:* `tests/test_baseline.py` double-run **SHA-256 sentinel** over streams⊕events⊕scores — **digest `9ea38949…336a5347`** (seed=42, build ×2 identical). **Full suite 35/35** (G1 6 · G2 3 · G3 4 · G4 6 · explainer 4 · smoke 3 · milestone 4 · timeline 4 · baseline 1); `ruff` clean (styx/app/tests/notebooks); both pages via AppTest no-exception. **Not committed — the MVP git tag is the user's action, on green.** | **210** (unchanged — no maths touched; G1/G3 re-run identical) | 2026-06-10 |
| S6 · R1a — history-as-prior · descriptive hazard (styx) | **Reach ✅ (claim-integrity)** | the first reach, **descriptive — no predictive-lift claim** (S5.5 settled: telemetry saturates AUC 1.000, no accuracy headroom). New flagged submodule `styx/reach/history.py` (config `REACH_HISTORY`), **augmentation not re-derivation**: time-to-escalation is **read off the existing risk waterline** (`cctx.risk` first crossing of the F4 threshold; recovered → right-censored at episode end 1440 min) — recomputes nothing in synth/forecast/risk, so the digest is unchanged **by construction**. `survival_table` → one right-censored row/patient (`pid, duration_min, event, density`, density=`sum(theograph)`); `stratify` fits a **single-covariate Cox PH** (parsimonious, ≤2 covariates on 21 events) → **HR 1.081/care-event, 95% CI [1.035, 1.128]** (clears 1 → denser history escalates sooner), **c-index 0.720** (discrimination, *not* a lift claim); a **median split** + log-rank → **p 0.0090** (strata separate); `KaplanMeierFitter` curves returned as pure `KMCurve` arrays for the R1b viz. lifelines 0.30.3 (Cox/KM/log-rank carry no RNG → DET-1). **Honest residual** = bottom-quartile density (`q25`, matching `ranking.new_low_history`) yet escalated → **{7, 39}**; **pid 39** (density 15, escalated 825 min) is the history-blind catch the live signal makes. `tests/test_reach_history.py` **4/4** (DET-1 over two builds; **single-source** — pid-39 duration == first `risk≥threshold` crossing; descriptive claim HR>1 ∧ CI clears 1 ∧ p<0.05 ∧ c-index>0.6 ∧ n_events==21; pid 39 ∈ residuals). **Fallback wired in the notebook** (degrade, never overclaim): if strata stop separating → ship standalone "history-only AUC 0.765" + pid-39, drop the stratification claim. Proof `notebooks/07_reach_history.py` renders the stratified KM curves + the HR/c-index/p panel + the pid-39 callout (runs clean as a script; py:percent, no `.ipynb` — repo convention). **Digest re-checked `9ea38949…336a5347` (unchanged)**; full suite **39/39** (35 prior + reach 4); `ruff` clean. Out of scope → R1b (viz builder + hazard panel alongside the untouched F4 waterline + the `explain.py` what/how/why entry). **Not committed.** | **210** (unchanged — reach reads the waterline, touches no maths; G1/G3 hold by construction) | 2026-06-10 |
| S6 · R4a — HERMES · lay explanation + patient-safe register (faithfulness gate) | **Reach ✅ (claim-integrity)** | the floor reach, **audience extension only — no predictive claim**. New droppable submodule `styx/reach/carer.py::lay_explain` (reach imports core, core never imports reach): a **read-only relabel** of the existing CALLIOPE real top-k into a carer/patient register — it **preserves rank and recomputes nothing** (single source: `styx.rationale.explain` produces the ranked, exactly-additive `top_k`; HERMES only renames). New **`CARER_NAMES`** in `styx/explain.py` (single-source, keyed by the CALLIOPE closed `VOCABULARY`): soft, non-alarming labels — "where the team would step in" not "breach"/"red zone"/"NEWS2", no "fires", no raw scores/σ; **added to the copy-lint union same commit** (`test_explainer::test_no_codename_in_copy` now scans `CARER_NAMES.values()`). The gate is **faithfulness** (patient-facing → *exact*, not ≥0.90): the lay top-1's underlying factor == the clinician CALLIOPE top-1, checked **pre- and post-breach** (CALLIOPE is regime-aware — `top_k` stays ranked in both regimes, HERMES reads `top_k[0]` regardless). **Faithfulness = 1.000 over 71 windows (50 pre-breach + 21 post-breach)**; the renderer carries a **collapse guard** — if the register were non-injective (two factors → one phrase) it raises rather than ship an ambiguous lay headline. `tests/test_reach_carer.py` **4/4**: HERMES-faithfulness 1.000 + rank-preservation; register **injective**; **patient-safe register** (no `breach`/`fires`/`red zone`/`news2`/`crisis`/`scale 1`/`threshold`/`escalat`/`alarm`/…, no codename, no raw score); DET-1 over two builds. **Worked example (silent case, idx 150, silent regime):** clinician *"Patient 0: falling oxygenation, approaching the silent hypoxia mode (risk 0.26)"* beside lay *"oxygen levels drifting toward where the team would step in"* — same primary driver (`oxygenation proximity`), same rank. Proof `notebooks/08_reach_carer.py` renders the faithfulness number + the register assertion + the clinician-vs-lay worked example (runs clean as a script; py:percent, no `.ipynb` — repo convention). **Digest re-checked `9ea38949…336a5347` (bit-identical — core maths untouched)**; full suite **63/63** (59 prior + reach 4); `ruff` clean (incl. notebook). Out of scope → R4b (carer page consuming the S5.5 timeline + this lay-CALLIOPE + one safe action, with the page-text register guard). **Not committed.** | **210** (unchanged — reach reads the top-k, touches no maths; G1/G3 hold by construction) | 2026-06-11 |
| S6 · R1b — history-as-prior · hazard panel (viz + page) | **Reach ✅ (R1 done)** | the R1 *view* — **additive, descriptive**, recomputes nothing. New `styx/viz/hazard.py::hazard_figure` (LYR-1 pure — data→Plotly, no Streamlit) draws the two KM step-curves (`line_shape="hv"`) from `stratify(cctx)`: denser (orange, solid) vs thinner (green, dashed — colour never alone, 6l), with the **focus patient's stratum bolded + annotated** ("This patient: thinner recent care history", placed off `median_density`, a new pure field on `HazardStratification`). **Stats off the face** (panel-review legibility): HR / c-index / log-rank go in the page caption (rendered live from `stratify` → no copy drift) + the `explain.py` "how"; the face carries only the curves + one plain finding line. New copy **single-sourced** — `KM_STRATUM_LABELS` in `styx/explain.py` feeds both `KMCurve.label` and the copy-lint; new `"history"` → **"History-based risk"** component (`COMPONENTS`/`DISPLAY_NAMES`/`EXPLAINERS`). **why-caveat present** (honesty-lint clean): descriptive association *not causal* (denser history flags sicker patients, it doesn't cause faster decline) · **no measurable lift over telemetry** · **misses low-history deteriorators (pid 7, pid 39)** — a stratum is a prior, not a safety guarantee. **User decision: stacked full-width below the waterline** (not a side column) → waterline / cone / **trajectory hero + its layout slot** untouched (S7 6d state-space drops in with no rework). Patient page stays logic-free — cached `_hazard()` fits the cohort once. `test_no_codename` **figure-arm self-maintains**: `hazard_figure` thunk added → `test_every_viz_builder_is_scanned` green (covers it), `test_viz_figures_carry_no_codename` green; `test_explainer` copy-lint extended with the stratum labels. **Digest re-checked `9ea38949…336a5347` (bit-identical — no `synth/forecast/risk` touched)**; **G1–G4 green**; full suite **59/59** (R1b extended existing tests rather than adding new fns → broader coverage, same count); `ruff` clean. **Not committed.** | **210** (unchanged — a view over the waterline; G1/G3 hold by construction) | 2026-06-10 |
| S6 · R4b — HERMES · carer page (patient-safe register, additive) | **Reach ✅ (R4 done)** | the carer-facing surface — **descriptive, additive, no predictive claim**; touches no clinician page and no core, so the digest is bit-identical **by construction**. New thin client `app/pages/03_carer.py` (LYR-1 — imports only `streamlit`+`styx`, computes nothing): a **fixed silent-window frame** (`ctx.default_idx`, shared `patient_pick` key, **no replay clock** → the page can never surface a future/post-threshold view). Four things, each sourced from `styx/`: (1) a **calm status** — new `styx.reach.carer.lay_status` maps the CALLIOPE *regime + top-1 dominance* to one of three score-free `CARER_STATUS` phrases (watching / steady / **involved** — the post-threshold honesty branch, so a carer is never told "nothing standing out" when crossed); (2) the **lay reason** — `lay_explain(frame.rationale).headline`, the **same primary driver** as the clinician CALLIOPE (R4a faithfulness **1.000**, regime-aware, unchanged); (3) a **"care so far" strip** — new pure `styx/viz/carer.py::carer_timeline_figure` **consumes the existing `episode_timeline`** (single-source, no recompute, no new maths): a monitored span + a "now" edge + the early-warning moment **only once already past** (`aegis.idx ≤ default_idx`) — it **omits the ETA band, the breach/threshold lane and the NEWS2 comparator**, so it carries no projection and no alarming term; (4) one **safe action** — `CARER_ACTION` (contact / what-to-watch, **no clinical instruction**), which also carries the lay scope anchor "being monitored is not the same as being safe". **Register calls:** dropped the clinician `SCOPE_LINE` from the page (vital-sign jargon + redundant with `CARER_ACTION`); the product name **STYX confined to the footer provenance** (neutral "Carer view" tab, "Patient {pid}" title — "Styx" has no place above a worried family). New copy single-sourced in `styx/explain.py` (`CARER_STATUS`, `CARER_TIMELINE_NAMES`, `CARER_ACTION`), each register-linted. **Both-arm guard (both matchers):** *figure arm* — `carer_timeline_figure` thunk added to `_figure_builders()` → **`test_every_viz_builder_is_scanned` green** (8 builders, 0 uncovered) + `test_viz_figures_carry_no_codename` green; *page-text arm* — new hand-maintained `test_carer_page_safe_register` drives the real page via `AppTest`. Each surface runs **both** the codename matcher **and** the banned-alarming set — authored copy via the **strict** matcher (full codenames incl. styx/hermes + raw-score regex), rendered page/figure via the **product-tolerant** matcher (drops styx/hermes + the raw-score regex, which would false-positive on the `v0.x` footer and Plotly axis floats). The page is raw-score-free **by design** (no `styx_index`/σ) — discipline, not the lint, keeps it so. **Worked frame (silent case pid 0, idx 150):** status *"one thing is standing out, so the team is keeping a closer eye for now"* (watching) · reason *"oxygen levels drifting toward where the team would step in"* (`oxygenation proximity` — the clinician top-1) · strip shows the early-warning marker (aegis 141 ≤ 150). **Digest re-checked `9ea38949…336a5347` (bit-identical — core untouched, empty diff on synth/forecast/risk/cohort/calliope/timeline/viz·timeline/01_/02_/app.py)**; **G1–G4 bit-identical**; full suite **71 passed** (63 prior + 8 new); `ruff` clean (incl. the new page). Phased R4b.1 (status+action+`lay_status`) / R4b.2 (`CARER_TIMELINE_NAMES`+figure) / R4b.3 (page+page-guard), each ≤~75 source LOC. **R4 (HERMES) complete.** **Not committed.** | **210** (unchanged — carer page reads outputs, touches no maths; G1/G3 hold by construction) | 2026-06-11 |
| S6 · R3a.1 — CADUCEUS · decoupling onset + cascade verify (mechanism gate) | **Reach ✅ (claim-integrity)** | the floor reach's **mechanism gate** — **temporal-mechanistic, no predictive/accuracy/lift claim** (S5.5 binding). New droppable submodule `styx/reach/decoupling.py` (reach imports core, core never imports reach): a **read-only** reader over the **existing G1 coherence computation** — `decoupling_onset(p)` returns the onset + the G1 lead off `synth.gates.windowed_coherence`/`decoupling_onset_index`/`decoupling_lead_min` (the same maths that produced the G1 lead 200), recomputing **nothing**; `cascade_verdict(cohort, p)` places that onset against the AEGIS fire read from the single S3 source `anticipation.fire_times`. **Single-source confirmed:** reach onset == `decoupling_onset_index` (sample 118 = **590 sim-min**), reach lead == `decoupling_lead_min` == **200** (onset 590 → first sustained single-signal breach 790). **Claim-integrity: PASS** — sustained ≥`_SUSTAIN`=2 windows below the pre-onset baseline−`_DROP_K`, `has_silent_window` holds (vitals in range, SpO₂ falling); a real, legible mechanism. **CASCADE VERDICT (the gate output): 3 STYX markers** (decoupling · AEGIS · F4) — **measured, not assumed**: decoupling onset **590** leads AEGIS **705** (at-cadence; raw 700) by a **gap of 115 sim-min ≫ one re-score cadence (15)** → decoupling is **detectably earlier** and carries its own marker. (This **overturns the pre-flight estimate** of near-coincidence/2 markers, which mis-derived onset≈715 from breach−200 using the F4 *threshold* 915; the G1 lead measures to the *single-signal* breach 790 → onset 590.) NEWS2 stays the external marker. The eventual 6-d state-space therefore carries **3 STYX markers**. `tests/test_reach_decoupling.py` **4/4** (DET-1 over two builds; **single-source** — onset/coherence/lead == the gates functions, `equal_nan`; mechanism-is-real — onset present ∧ silent window ∧ lead ≥ floor 90; cascade — 2\|3 consistent with the 15-min margin rule). Proof `notebooks/09_reach_decoupling.py` renders the coherence face + onset marker, the single-source check, the claim-integrity verdict, the cascade verdict, and the digest re-check (py:percent, no `.ipynb` — repo convention). **Digest re-checked `9ea38949…6a5347` (bit-identical — core untouched)**; G1/G3/baseline 11/11; full suite **75/75** (71 prior + 4); `ruff` clean (incl. notebook). Out of scope → R3a.2 (the coherence/decoupling mechanism panel on the patient page, marker treatment = **3** per this verdict; + `REACH_DECOUPLING` flag + `explain.py` what/how/why entry). **Not committed.** | **210** (unchanged — reach reads coherence, touches no maths; G1/G3 hold by construction) | 2026-06-11 |
| S7 · 6d.1 — clinical state-space trajectory (hero port, static · viz polish) | **Polish ✅ (descriptive)** | the rendered patient-page **hero swapped** from the constructed/model-axes `trajectory_figure` to a new pure builder `styx/viz/trajectory.py::clinical_trajectory_figure` (LYR-1 — data→Plotly, no Streamlit/I/O/maths) — the patient's path through the **literal SpO₂ × RR plane**. **Ported from the user's matplotlib schematic (design, not its numbers): all geometry is data-driven** — markers sit on the *real* silent-case trajectory at each fire-time, read from existing sources (`decoupling_onset.onset_min` **590** · `ctx.fire.aegis_min` **705** · `ctx.fire.threshold_min`/F4 **915** · `news2_crossing` **1010**); "now" parked at the late NEWS2 breach (static retrospective view; the interactive scrub is 6d.2). **Warm-ramp background single-sourced** from `readouts._spo2_scale1_score`+`_rr_score` (the shading *is* the NEWS2 Scale-1 sub-score over the plane — no duplicated thresholds, no new maths) via new `palette.WARM_RAMP`. **Framing guards held (verified in the rendered strings):** marker 1 = **decoupling as mechanism** ("coupling breaks down here", not an alert); lead bracket = **early-warning-vs-NEWS2 only** ("≈ 5 h lead · early warning 705 vs NEWS2 1010") — no decoupling-based lead, no "590 vs 1010"; **no clickable Clinical/Model toggle** (a muted "Model view — on the roadmap" caption only); marker 3 reframed "escalation crossing" (dropped "stage semantics — confirm"); ECHO → "**past-deterioration cluster**" (escalated look-alike endpoints, **no codename**); retrospective-replay + synthetic-replay honesty footnotes present. **`trajectory_figure` retained** as the future model view (kept in the module + test-scanned; just not rendered). **Floor panels untouched** (timeline · waterline · R1 hazard · R3a coherence · cone). `explain.py` `trajectory` what/how/why **rewritten** for the clinical view (honesty-lint + no-overclaim green; "trajectory" key reused → completeness intact). **Meta-test covers the new builder:** `clinical_trajectory_figure` thunk added to `_figure_builders()` → `test_every_viz_builder_is_scanned` green (11 builders, 0 uncovered) + `test_viz_figures_carry_no_codename` green on it. **Digest re-checked `9ea38949…6a5347` (bit-identical — no `synth/forecast/risk` touched)**; G1–G4 green (19/19); full suite **76 passed**; `ruff` clean (styx/app/tests). Phased A (warm field+chrome) / B (path+markers+brackets+ECHO+legend+footnotes) / C (page swap+explain+thunk+log). Out of scope → 6d.2 (interactive scrub: drive "now" + marker reveal off the replay clock, tooltips). **Not committed.** | **210** (unchanged — pure viz over existing outputs; G1/G3 hold by construction) | 2026-06-11 |
| S6 · R3a.2 — CADUCEUS · decoupling mechanism panel (patient page, additive) | **R3a done ✅ (descriptive)** | renders the **mechanism** behind the silent deterioration — the RR–SpO₂ breathing–oxygen coherence — as a patient-page panel, **additive** (trajectory hero + its slot untouched; the S7 6-d state-space drops in with no rework) and **mechanistic/descriptive only** (no predictive/accuracy/lift claim). New **pure builder** `styx/viz/coherence.py::coherence_figure(t_min, coherence, onset_min, *, aegis_min=None)` (LYR-1 — no Streamlit/I/O/maths): one coherence-trace line (`connectgaps=False` → the warm-up NaNs read as a gap), the **decoupling-onset marker** framed as the *mechanism's onset* ("breathing–oxygen coupling breaks down here", solid `ANNOTATION` rule), and a **light early-warning context marker** (dotted `EARLY_WARNING`, width 1) so the mechanism→alert relationship is visible. Consumes the existing G1 series via `reach.decoupling.decoupling_onset` (single source — recomputes nothing) + AEGIS off `ctx.fire.aegis_min`. **Framing guards held:** onset = mechanism onset (not STYX's earliest alert); **no decoupling-based lead caption anywhere** (no "590 vs 1010", no "≈7 h"); **headline lead stays early-warning-vs-NEWS2 (≈5 h)**; no F4/NEWS2 lane drawn (that's the 6-d state-space's job). Patient page panel **guarded** (selector allows any escalator; `decoupling_onset` raises when no onset) and inserted after the history (R1) block, hero block untouched. `explain.py`: activated reserved `caduceus → "How the vital signs move together"` (added to `COMPONENTS`) with what/how/why — the **why carries the guards** (mechanism-not-alarm · STYX alerts at the early warning · headline stays early-warning-vs-NEWS2 ≈5 h · no extra accuracy · synthetic-replay anchor); honesty-lint + `test_no_overclaim` green. New `COHERENCE_LABELS` register dict (plain copy, no codename) folded into the copy-lint union. **Meta-test covers the new builder:** `coherence_figure` thunk added to `_figure_builders()` → `test_every_viz_builder_is_scanned` green (10 builders discovered, 10 thunked, 0 uncovered) and the **no-codename figure arm passes on it**; `test_no_codename_in_copy` green incl. the new labels. **Digest re-checked `9ea38949…6a5347` (bit-identical — core untouched)**; G1–G4 + baseline green; full suite **75/75** (unchanged — extended existing test arms, no new test fn); `ruff` clean. ~100 LOC (at the slice limit; ~16 of it docstring/comment). **Not committed.** | **210** (unchanged — additive render over read-only coherence; G1/G3 hold by construction) | 2026-06-11 |
| S7 · 6d.2 — clinical trajectory · scrub + tooltips (interactive, closes 6d) | **Polish ✅ (descriptive)** | the hero goes **scrub-driven** — viz + page UI only, **no maths**. `clinical_trajectory_figure` gains `now_idx: int \| None = None` (sample index, same convention as `cone_figure`; `None` → 6d.1 parked-at-breach, so the meta-test thunk + any notebook caller are unchanged *and* the default render keeps every tooltip string inside the no-codename figure scan): the path is drawn up to `now_i`, the "now" ring sits there, and the four cascade markers **reveal progressively** once `fire-time ≤ now` (decoupling **590** → early warning **705** → escalation/F4 **915** → NEWS2 **1010** on the silent case). Builder stays pure (LYR-1 — takes `now`, renders; no clock logic). **No new slider:** wired to the **existing replay clock** (`scrub_pos` select_slider, silent-window default on patient change, Silent/AEGIS/Breach/Step buttons) — one scrub now moves **trajectory + cone + CALLIOPE/metrics together** (ward board shares the same session key). Default position = **silent window** (the money shot: markers 1+2 fired, F4 + NEWS2 still ahead). **Tooltips bank the crowding relief:** markers → plain event name + sim-min + SpO₂/RR at fire-time (`customdata`); the path hover carries the **measured** vitals + sim-min via 3-col `customdata` (the drawn line is smoothed — presentation only, so hover shows raw, never smoothed); "now" ring → sim-min; the bottom marker-legend strip **lightened** to colour semantics + "hover each for detail" (numbered badges kept for at-a-glance time order). **Framing guards held:** marker 1 stays the *mechanism* ("coupling breaks down here"); lead bracket stays **fixed retrospective** early-warning-vs-NEWS2 ("≈ 5 h lead · 705 vs 1010") — eyeballed at the silent default and it reads as the episode's known punchline (consistent with the replay caption), so the reveal-gated fallback wasn't needed; hero teaching caption now **guarded** — narrated only over the *revealed* stretch, suppressed pre-cascade (rule 6: never narrate what "now" hasn't reached); retrospective caption position-aware ("parked at the breach" when static, "follows the replay clock" when scrubbed) + synthetic-replay anchor kept. **Copy-lint union extended same commit:** the four marker hover labels moved to `explain.py::TRAJECTORY_MARKERS` (imported by the builder) and folded into `test_no_codename_in_copy`. New proof `tests/test_trajectory_scrub.py` (3 tests): reveal in cascade order incl. empty pre-cascade, `now_idx=None` ≡ parked-at-breach all-four, tooltips carry sim-min + vitals. Eyeballed via kaleido PNGs at early/silent/breach. **Digest re-checked `9ea38949…6a5347` (bit-identical — no `synth/forecast/risk` touched)**; G1–G4 green (19/19); full suite **79 passed** (76 + 3 new); `ruff` clean (styx/app/tests). ~95 LOC incl. test + log (single phase). **6d closed.** **Not committed.** | **210** (unchanged — pure viz over existing outputs; G1/G3 hold by construction) | 2026-06-11 |
| S7 · 6e — watchlist urgency tiers (ward board; panel F-02 alert-fatigue fix) | **Polish ✅ (descriptive)** | the flat 21/50 watchlist splits into three urgency tiers — **Review now / This hour / Watch** — a **pure classification over existing signals** (no new score): `styx/cohort/ranking.py::watch_tier` reads the status trichotomy, the early-warning flag and the **ordinal ETA band** (`styx.readouts.eta_ordinal` — the same bands the board prints, no raw minutes re-encoded). Criteria: review-now = escalated ∨ band <30 min (the escalated clause is **unsatisfiable within the watchlist** — membership requires risk below the line — kept for frame-generality and asserted in test; the **user-facing caption is trimmed to the satisfiable half** — "escalation projected within 30 min" — so a nurse is never sent looking for crossed patients this list excludes by definition; crossed patients keep their separate home atop the triage board, verified disjoint at every frame); this-hour = early-warning fired ∧ 30–60 min; watch = the rest. **Frame-dependent off the shared `scrub_pos` clock** (one `watch_tier` per `ward_frame` row per scrub; tier groups simply partition the existing soonest-first sort, so focus mode's shared TOP_N budget renders exactly the rows it showed before, consumed in tier order). Ward page renders grouped sections with a distribution caption + per-tier count headings + **visible criteria captions** (transparency standard); raw tier keys never render — `explain.py::WATCH_TIER_LABELS` / `WATCH_TIER_CRITERIA`, **copy-lint union extended same commit**; `EXPLAINERS["watchlist"]` how/why amended for the split (honesty-lint green). **Distribution at the silent-window default frame: of 21 → 2 review-now · 2 this-hour · 17 watch** — non-degenerate, all tiers populated, the triage head manageable and the bulk quiet (asserted: review-now < watch). New proof `tests/test_watch_tiers.py` (5 tests): criteria mapping incl. the defensive escalated clause, non-degenerate default-frame split, DET-1 (seed=42 ×2 identical (pid, tier) lists), tier-key/label/criteria closure + raw-key leak guard, rendered-page labels+criteria via AppTest. No new Plotly builder → viz meta-test untouched. **Digest re-checked `9ea38949…6a5347` (bit-identical — no `synth/forecast/risk` touched)**; G1–G4 green (19/19); full suite **84 passed** (79 + 5 new); `ruff` clean (styx/app/tests). ~50 non-test LOC (single phase). **Not committed.** | **210** (unchanged — pure relabelling over existing outputs; G1/G3 hold by construction) | 2026-06-11 |
| S7 — NEWS2 A/B (STYX vs standard-of-care, patient page) | **Polish ✅ (descriptive)** | the head-to-head, **additive + descriptive/retrospective** — composes two *existing* computations on one clock, recomputes nothing. New pure builder `styx/viz/comparison.py::comparison_figure` (LYR-1): **stacked shared-x lanes** (not a dual y-axis — incommensurable scales on one plot invite rigged-axes readings; each score sits against *its own* escalation line and the lead is read off the shared clock, never off amplitude). Top lane = the F4 risk (`ctx.risk`, blue) + the 0.5 escalation line; bottom lane = the **partial NEWS2 step trace single-sourced from `readouts.news2_partial`** (passed through unchanged — fair, never re-rigged/weakened), comparator black, trigger line at ≥5, **fixed honest y-range 0–12** (never zoomed to flatter the flat line). Crossing markers sit on the real series at each fire-time (STYX × at **915**, NEWS2 hollow vermilion diamond at **1010** — the 6d marker convention); early-warning rule at **705** spans both lanes. **The divergence shape is real on the data:** through the silent window the partial NEWS2 never exceeds **2** while the risk climbs 0.15→0.56. **Framing guards held (verified in rendered strings):** lead bracket = **early-warning-vs-NEWS2 only** — "early warning leads NEWS2 by ≈5 h (305 min) **on this presentation**" (no decoupling-based lead, no universal claim); **reveal-gated** — markers/rule/bracket appear only once "now" passes each fire-time (rule 6; stricter than 6d's ungated bracket — flagged, not bundled); panel caption = the existing **`NEWS2_PARTIAL_LABEL` ("4 of 7…") reused** + a scoped-claim sentence ("…a synthetic replay of one scenario, not a general claim about NEWS2"). Panel wired **additively** after the waterline on the shared `scrub_pos` clock; new `"comparison"` component ("STYX vs NEWS2, side by side") with what/how/why carrying the guards (partial · scoped · early-warning-based lead · synthetic-replay anchor); new `COMPARISON_LABELS` register dict **folded into the copy-lint union same commit**; `comparison_figure` thunk added → `test_every_viz_builder_is_scanned` green (12 builders, 0 uncovered) + no-codename figure arm green. New proof `tests/test_comparison.py` (3 tests): lead annotation **single-sourced** from the fire-points + headline pin **305.0**; NEWS2 lane array-equal to `news2_partial` (never re-scored in the figure); nothing revealed pre-clock. **Digest re-derived `9ea38949db8e5b8c19f969b9919d804013285fb78e0e48f5449c7e76336a5347` (bit-identical — no `synth/forecast/risk` touched)**; G1–G4 green (**19/19**); full suite **87 passed** (84 + 3 new); `ruff` clean (styx/app/tests). Phased a (builder + labels + both lint registrations) / b (page + explainer card + proof tests), each ≤~100 source LOC. **Not committed.** | **210** (unchanged — pure viz over existing outputs; the panel's 305 is the separate early-warning-vs-NEWS2 number, never conflated with this AEGIS→threshold lead) | 2026-06-11 |
| S7 · 6k — cards by ward (two-preset configurable; declutter + drill-through) | **Polish ✅ (descriptive)** | the ward board becomes a **tiered, ward-grouped card deck** — presentation only, no new score. **Ward index is derived, presentation-layer** (the cohort carries **no ward attribute** — a planning correction to the 6k brief): new `styx/cohort/wards.py::ward_of(pid) = pid % WARD_COUNT` (`WARD_COUNT=3` in config; pure, reads nothing but the pid → **digest-safe by construction**, `synth/` untouched). **Two setting presets single-sourced in `styx/explain.py`** — `WARD_PRESET_NAMES` / `WARD_LABEL_PRESETS`: **NHS hospital-at-home** (default, by dict order): Respiratory · Frailty · Heart failure; **Role 3 field hospital**: Post-ITU step-down · Surgical · Medical — sidebar selectbox (`key="ward_preset"`) flips labels over the same cohort, nothing re-scored; **both label sets + preset names folded into the copy-lint union same commit**. *Layout (`02_ward.py`):* the flat 6e watchlist **and** the F6 triage dataframe replaced by **three bordered ward boxes**, ward-first with **urgency-within** — `watch_tier` drives card grouping + a tier badge (new `palette.WATCH_TIER_BADGE`, colour a redundant cue per 6l; `ward_frame`'s soonest-first order preserved inside each tier, so rank still orders the cards); box header = patient count + **review-now count**; the watch tier **collapses to "+ N watching"** (`st.expander`); **hero card** (the silent case, `cctx.cohort.silent_case().pid` — derived, never hardcoded) highlighted by card border; tier legend caption keeps the 6e labels+criteria transparency (its render test green untouched). *ECHO panel removed from the page* (focus selectbox + `echo_neighbours` call + figure render; the **focus-mode toggle and TOP_N budget retired** with it) — **`echo_figure` retained** in `styx/viz` + its thunk, so `test_every_viz_builder_is_scanned` + the no-codename figure arm stay green (the `trajectory_figure` pattern); `"echo"`/`"watchlist"` stay registered in `EXPLAINERS` (no render-coverage test; `ward_board` card reworded for the deck, honesty-lint green). *Drill-through:* each card reuses the existing `_drill` (sets `patient_pick` + `scrub_pid` + `scrub_pos`, then `st.switch_page` → lands on that patient at the same clock t — the milestone clock-carry test unchanged), **gated to escalators only**: Streamlit silently resets an out-of-options `patient_pick` to the default, so a stable card drilling would open the *wrong* patient — verified in the 1.58 selectbox source, asserted in test. **Live-click fix (latent since S5, exposed by the deck's many buttons):** `_drill` assigned `st.session_state["scrub_pos"]` *after* the slider keyed `scrub_pos` was instantiated → `StreamlitAPIException` on a real click (the milestone drill test seeds the patient page, so it never clicked). The write was removed; the carry needs one legal line instead — a widget-owned key is **dropped on a page switch** (its widget didn't render), so both pages now **promote the clock to session-owned** before their slider (`st.session_state["scrub_pos"] = st.session_state.get("scrub_pos", default_pos)` — also subsumes the old missing-key init); audit: every other `scrub_pos` write on both pages precedes its slider. **Verified live** (headless Chromium on `streamlit run app/app.py`): ward scrubbed 750→915, drill → patient 0 opens at **915 min · BREACH**, pid selected; round-trip ward 825 → patient 825 → back to ward **825 kept**; Silent-window reset works; drill from inside the "+ N watching" expander carries too; no StreamlitAPIException anywhere. New proof `tests/test_ward_boxes.py` (6 tests): pid-partition + hero-in-box-0, preset-map closure (NHS first), 3-box render (no table/chart, NHS labels, Role-3 absent), preset flip relabels, drill buttons == escalator set, and a **real-click regression** (clicks `open_0`, tolerates only AppTest's known `switch_page` page-resolution failure, asserts `patient_pick`/`scrub_pid` set + `scrub_pos` untouched); milestone F6/F10 evidence retargeted (F6 = the three boxes render + 0 dataframes/0 charts; F10 = the `echo_neighbours` retrieval sanity). **Digest re-checked `9ea38949db8e5b8c19f969b9919d804013285fb78e0e48f5449c7e76336a5347` (bit-identical — no `synth/forecast/risk` touched)**; G1–G4 green (**19/19** + baseline sentinel); full suite **93 passed** (87 + 6 new); `ruff` clean (styx/app/tests). Phased P1 (derivation + presets + lint) / P2 (ECHO removal) / P3a (deck) / P3b (dataframe drop + tests), each ≤~85 source LOC. **Not committed.** | **210** (unchanged — presentation over existing outputs; G1/G3 hold by construction) | 2026-06-12 |
| S7 — carer page → **Patient Display** (rename + register lift) | **Polish ✅ (copy only)** | the family surface renamed: `app/pages/03_carer.py` → `03_patient_display.py` (sidebar "patient display", `page_title="Patient Display"`; one test path updated). Authored carer copy lifted to a **formal but lay-safe** register — sentence-cased full sentences, NHS-leaflet tone — across `CARER_STATUS` / `CARER_NAMES` / `CARER_ACTION` / `CARER_TIMELINE_NAMES` (`styx/explain.py`) and `STABLE_HEADLINE` (`styx/reach/carer.py`); `CARER_FOOTER` unchanged. **R4 patient-safe vocabulary kept** (banned-term lint untouched: no threshold/escalation/deterioration/NEWS2/alarm/codenames on this surface); `CARER_NAMES` stays injective; the "watching more closely" timeline literal preserved. Page gains a "What is standing out" section heading + reworded timeline caption; thin client, no logic. HERMES faithfulness **1.000** holds; full suite **93 passed**; no `synth/forecast/risk` touched → G1/G3 hold by construction. **Not committed.** | **210** (unchanged — copy + rename only) | 2026-06-12 |

**R4b notes.** *The softest surface — descriptive, never a prediction.* R4b ships the carer page as an
audience extension, not a new claim: it renders one **fixed silent-window frame** (no clock, so it
structurally cannot show a future/post-breach view), reuses R4a's faithful lay-CALLIOPE (the carer line
names the **same** primary driver the clinician sees, faithfulness 1.000), and draws a "care so far"
strip that **consumes the existing `episode_timeline`** and shows only what has already happened — the
monitored span, "now", and the early-warning moment once past — dropping the ETA projection, the breach
lane and the NEWS2 comparator so no predictive or alarming claim can reach a family. *Register beats
reuse.* Two clinician affordances were deliberately **not** carried across: `SCOPE_LINE` (vital-sign
jargon, and redundant with the safe-action's "monitored ≠ safe" anchor) and the product name on the
page chrome ("Styx" — the river of the dead — has no place above a worried family; it stays in the
footer provenance only). *Two matchers, two configs.* Authored carer copy is linted with the **strict**
matcher (full codenames incl. styx/hermes + raw-score); rendered surfaces with a **product-tolerant**
matcher minus the raw-score regex (which would false-positive on the version float and Plotly axis
numbers) — the page stays score-free **by design**, not by lint. The figure arm self-maintains (the
meta-test would fail with `carer_timeline_figure`'s name until its thunk was added); the page-text arm
is the hand-maintained one, so the new page was added explicitly. **R4 (HERMES) is complete; next is the
reach order R3 → R2.**

**R1a notes.** *The first reach — description, not lift.* The S5.5 go/no-go settled it: telemetry saturates in-sample (AUC 1.000), so R1 claims **no** predictive lift. What it adds is a **hazard stratification** — denser Theograph history → shorter time-to-escalation — reported the honest way for survival data (**hazard ratio / log-rank / c-index**, never a binary AUC), parsimonious (one covariate on 21 events). *Single-source, by construction.* Time-to-escalation is **read off** the existing risk waterline (first crossing of the F4 threshold), so the reach derives no new risk; recovered patients right-censor at episode end. Because nothing in `synth/forecast/risk` is touched, the pipeline digest is re-checked identical (`9ea38949…336a5347`) and G1–G4 hold without re-derivation — the reach is a *view over* the MVP, not a competing predictor. *The honest residual.* pid 39 (bottom-quartile care history, yet escalated) is the explicit limit of a history prior — exactly the patient the MVP's `new_low_history` watch-flag surfaces and the live signal catches; pid 7 joins it in the residual set. R1 is a prior, not an oracle, and the proof says so. *Claim-integrity discipline.* The primary path is asserted in the test; the fallback ("history-only AUC 0.765" + pid-39, stratification claim dropped) is wired behind an `if not separated:` branch in the notebook so the claim degrades rather than overclaims. *Scope held to ≤100 LOC:* styx module + test + proof only — the viz builder, the hazard panel alongside the untouched F4 waterline, and the honesty-linted `explain.py` entry land in R1b.

**R1b notes.** *The R1 view — additive, never a second predictor.* R1b renders the R1a stratification and nothing more: `hazard_figure` consumes the `HazardStratification` dataclass (it recomputes no risk), and the page caches one cohort-wide `stratify()` — so the determinism digest is bit-identical **by construction** and G1–G4 hold. The waterline, the forecast cone and the trajectory hero (with its layout slot, reserved for the S7 state-space) are untouched; the only edits to `styx/reach/history.py` were copy (the stratum labels now read from `KM_STRATUM_LABELS`) and a pure `median_density` field — no maths. *Legibility over statistics.* The panel face shows two KM curves + this patient's stratum + one plain finding line; the HR (1.08), c-index (0.72) and log-rank (p .009) live in the caption and the ⓘ explainer, not on the chart — a hub nurse reads the picture, the analyst expands the numbers. *Honesty carried in the copy.* The `explain.py` "why" states the three caveats explicitly — the association is descriptive not causal, it adds no measurable accuracy over the live vitals here, and it misses thin-history deteriorators (pid 7, pid 39); the residual is the same one R1a logs. *Plain-only, self-maintaining.* The new stratum labels are single-sourced in `KM_STRATUM_LABELS` and run through the copy-lint, and the no-codename figure arm now scans `hazard_figure` (the meta-test would have failed with the builder's name until the thunk was added) — a future reach panel cannot silently bypass the guard. *Layout.* Stacked full-width below the waterline (user call), so both time-series stay legible on a staff screen. **R1 (history-as-prior) is complete**; next is the reach order R4 → R3 → R2.

**S5.5 notes.** *Surface-only, proven by the sentinel.* P1/P2 add a timeline view and a measurement notebook; neither touches `synth/state/forecast/risk/rationale`, so the SHA-256 pipeline digest is the regression proof (one number to re-check, stronger than scattered `array_equal`s). The one shared-file edit — promoting `_eta_band` → public `eta_band` in `cohort/ranking.py` — is a rename + re-export only (the ward board's call site updated; logic byte-identical), so it touches no maths and the ward tests still pass. *The R1 go/no-go (decisive).* Telemetry at the silent-window frame already separates the scripted escalators perfectly in-sample (AUC 1.000), so there is **no headroom** for history to add marginal predictive value — `combined − telemetry ≈ 0`. This is a construct artifact (the synthetic risk at `di` already encodes the scripted outcome), not a real-ward result, but it *bounds R1's framing*: **R1 = a hazard/explanatory view over the cohort** (history explains the baseline), **not** "history improves prediction." Settle this before R1's first slice (parked S6-P0 #1). *Honesty on the beats.* The quietest patient is genuinely calm (stable, no forecast) — the silent-deterioration story is patient 0 (in-range, AEGIS fired), and the notebook says so rather than overclaiming the single calmest. *ETA legibility.* The strip draws the projected escalation as a bounded band only when the point forecast crosses (`eta_confident`); otherwise an open-ended "≥ soonest" marker — patient 0 at the silent window is the open-ended case (soonest ~70 min, central None), so it reads as an uncertain window, not a hard minute (UQ-1).

**S5 notes.** *Surface, not maths — by construction.* F6/F10 only read the cohort's existing risk /
forecast / AEGIS / embedding outputs, so the Hard-Rule-4 invariants are untouched and the re-run is
identical, not merely "in band." The one move that *did* touch a shared file (the labs drop) was kept
deliberately surgical — config-only, leaving `generate_episode` still drawing (and returning) labs so
every RNG draw downstream lands in the same place; `VITALS`-keyed consumers (`_vital_matrix`,
`exceedance_per_vital`, `ward_frame`'s `max_exc`) simply stop iterating over it. The decisive check was
`emb.mode`: labs was PC1's nuisance axis, so removing it *could* have flipped the PCA fork legible and
moved the basins — it didn't (still `constructed`), and G2/G3 prove it. *ETA as a band, on purpose:* the
cone's point forecast often never reaches the line within the horizon even when the upper edge does
(patient 0 at the silent window: soonest ~70 min, central None → "~70+ min, low confidence") — reporting
a single minute there would manufacture confidence the model doesn't have, so the trichotomy + `eta_confident`
carry the honesty. *The default frame is intentionally escalated-free:* at the silent-window money shot
nobody is over the line (15 escalating + 35 stable, 21 on the watchlist); the escalated bucket appears
only as you scrub forward (idx 183: 9 escalated / 12 escalating / 29 stable) — which is the whole point,
the watchlist surfaces deterioration a threshold board still shows green. *Conformal band choice:* the
ward pools the band over the **whole** cohort (one band, marginal coverage) where `frame.py` uses
leave-one-out per patient — a deliberate cost/consistency trade; the methods notebook validates the
pooled band hits 0.914 empirical vs 0.90 nominal. *Promote-to-occult-archetype* (labs as a second hero)
is synth-touching and gate-cascading → correctly deferred past the tag, not part of this slice.

**S4.5 notes.** *The bug was rendering, not maths.* G4's 138 points were all sound — the additive
identity holds pre-breach (idx 150: 0.23+0.03+0 = 0.26 ✓); the captured frame was simply out-of-
distribution for the tested invariant (post-breach the proximity overshoots the attractor and the
per-axis term clips, so Σcontrib 1.30 ≠ risk 1.00). Fix is a `additive` flag gating the panel +
a regime-aware verb + a σ word-clamp — zero change to `risk_series`/accessors, so G1/G3 are
untouched by construction. *`regime` vs `additive` correctly decouple:* the verb flips at the
escalation threshold (risk ≥ 0.5, idx 183), but `additive` only goes False later, once the proximity
actually overshoots (idx ~285) — so contributions still render in the 183–~280 band where they
genuinely sum, and suppress only where they don't. *Explainer location (LYR-1 call):* landed in
`styx/explain.py` as pure, testable content (the text analogue of `styx/viz`), not `app/` — it
imports no streamlit and holds no behaviour, and the gate-style honesty lint mirrors
`test_vocabulary_is_closed`. *Scrub:* shipped jump+ticks+step (robust, demos predictably); the
`st.fragment` auto-play timer was declined to avoid rerun flakiness in a live room.

**S4 notes.** *Faithfulness oracle (the load-bearing choice).* A naïve vital-perturbation ablation
scored only 0.50 — it can't separate the two **co-driven** additive terms (perturbing RR removes
both its effort-*proximity* and its *exceedance*), so it's an unsound oracle. The right independent
ground truth is the synthetic **archetype** (the generating mechanism, which the attribution never
sees): silent-hypoxia→oxygenation, compensated→effort, coupled→either. Against it the named top-1
agrees 0.993 on the held-out silent window. The decomposition is **exactly additive** (the per-axis
proximity terms provably sum to `_proximity`), which is what makes top-1 unambiguous — the property
that keeps the rate off the tie-wobble cliff the plan flagged. *Compensated wrinkle:* its effort
trend breaches fast, so it has almost no pre-breach silent window — only one evaluable re-score in
the whole cohort, a near-tie at onset (oxygenation momentarily edges effort). Honest, immaterial to
the headline. *Ghost decision:* shipped the **stale-forecast** ghost (re-run from the AEGIS anchor),
not the PRD's no-intervention counterfactual — the latter needs a counterfactual stream from `synth`
(Hard Rule 4/6), deferred. *Completeness scope:* contributions reconstruct risk on the silent window
(risk ≥ 0.1, pre-breach); post-breach the proximity **overshoots** the attractor and the term clips,
so the additive identity is asserted only where neither term saturates — the regime CALLIOPE exists
to explain.

**S1-enrich / S2-refit notes.** The first S2 fell back to constructed because every escalator was the
*coupled* shape — oxygenation and effort moved together on one diagonal, so the state space was
effectively 1-D, `labs_proxy` was dead weight (PC1 nuisance), and the engine never produced silent
hypoxia (the AEGIS/F7 phenomenon). Fix in the **engine**: `generate_episode` now takes an `Archetype`
and splits the post-onset slopes — SILENT_HYPOXIA (SpO₂ full, RR/HR ≈0), COMPENSATED (RR/HR up, SpO₂
shallow), COUPLED (legacy). All keep the coupled pre-onset regime so the coherence collapse is still
detectable. `build_cohort` scripts patient 0 = silent hypoxia and draws each escalator's shape uniformly
from the seeded child stream (DET-1 preserved; patient-0 stream untouched, comorbidity draw still
trailing). `has_silent_window` generalised to a multivariate adverse-trend test (user-locked) since
silent hypoxia has RR flat. **Outcome path unchanged** (sigmoid of frailty over the observable
`comorbidity_index`) so AUC stayed in band (0.765) — the richer vitals don't leak into the label.
**G2 still ships constructed**: even with off-diagonal archetypes, the cohort's dominant variance is the
shared "severity" direction (oxy down + effort up), so PC0 keeps absorbing both — *not* worth over-tuning
the engine to flip the mode (per plan). The substantive win landed anyway: the named oxy×effort plane now
carries real 2-D structure (silent vs compensated separate **only** on effort), which is the differentiator
a single risk line can't show and the shape ECHO will later match. Labs given its own later onset so it
feeds S3 risk as semi-independent signal without hijacking the map. Re-run G1+G2 after any further
`synth/forecast/risk` change (Hard Rule 4). LOC (changed): `scenario` 107 / `cohort` 128 (+~25) /
`synth/gates` 146 (−4) / `synth/__init__` 27; Phase B = **zero** `styx/` change (auto-fork), notebooks only.

**S2 notes.** F1 = a deterministic linear map: standardise the 5-D `VITALS` vector, project onto
two axes. `fit_embedding` is PCA-first with an auto-fork — it builds the PCA candidate, labels/orients
each axis by its construct correlation, then asks the canonical gate helper (`axis_construct_corr` +
`is_legible`, deferred-imported to keep the `gates → embedding` layering) whether the axes are legible.
On seed=42 PCA **failed** the fork and fell back to the hand-built oxygenation × effort projection: the
synthetic deterioration drives SpO₂ down *and* RR/HR up together, so the two named constructs are
strongly anti-correlated and PC0 (75% var) captures both at once (oxy −0.97 / effort +0.98), leaving
PC1 (15% var) on the labs_proxy nuisance axis (r≈0.10) — two axes, one construct, illegible. The
constructed fallback is legible by construction (oxy r=1.000, effort r=0.996) — a valid G2 pass, and
exactly the scenario the fork exists for. Basin = mean/σ of in-range samples (any patient); attractor =
mean/σ of breach samples among escalators; `trajectory_drift` projects the silent case's onset→breach
travel onto the basin→attractor unit vector (+1.378, toward crisis — the G1↔G2 link). S2 reads the
cohort only, so Hard Rule 4's `synth/` invariant is untouched: G1 re-run 6/6, lead unchanged at 160.
LOC: `constructs` 30 / `embedding` 132 / `gates` 49 / `viz/trajectory` 45 / `state/__init__` 29 /
`test_g2` 35 (+config 6). The five-file set is what `BUILD_MVP.md` prescribes for S2; `embedding.py` is
the cohesive hub (mirrors S1's `gates.py` precedent). UMAP/VAE deferred (UMAP breaks DET-1; VAE = reach).

**S1-refine notes.** AUC=1.000 made history deterministic of outcome (clinically unreal; would
make telemetry + R1 redundant in-silico). Fix: outcome is *sampled* from a sigmoid of frailty
(`_OUTCOME_K=5.0`), so frailty raises the *odds* of escalation, not the certainty; the AUC is
scored against an observable `comorbidity_index` (= observed event density + measurement noise,
σ=3), never the latent frailty — so it reflects what a real model could see. Determinism held by
keeping patient 0 free of frailty/outcome draws and making the comorbidity draw *trailing*
(verified patient-0 vitals identical across n=12 and n=50 → lead invariant). 60-seed lead sweep:
all ≥90 (min 90, median 145). 60-seed AUC: 0.59–0.89 (informational; seed=42 contract = 0.782,
comfortably centred). Re-run G1 after any `synth/forecast/risk` change (Hard Rule 4).

**S1 notes.** COPD-exacerbation scenario: latent frailty → Theograph event density (Poisson)
→ conditioned physiology + crisis propensity → labelled outcome (causal chain, so history is
learnable). Decoupling = loss of the fast homeostatic RR–SpO₂ co-fluctuation; measured by
*detrended* windowed coherence so the slow opposite trends don't masquerade as coupling. Lead
= onset (sustained coherence collapse) → first *sustained* single-signal breach (SpO₂<94);
silent window = onset → first instantaneous excursion (strictly in-range, trend adverse). Robust
≥90-min lead across 60 seeds (min 110), not just seed=42 — the index case is scripted, not gambled.
LOC: `scenario` 86 / `cohort` 87 / `gates` 143 / `__init__` 25 lines; `gates.py` exceeds the
~80-line target as the cohesive gate-helper hub (split would fragment the G1 measurement logic).
G1 sub-conditions must be re-checked after any `synth/forecast/risk` change (Hard Rule 4).

**S5.6 — UX hardening before R1 (surface-only).** Carer-page + reach conventions fixed before the
reaches inherit them: P0 strip debug `st.write` / build+seed footer / 2×2 non-wrapping replay
buttons; P1 STYX-index anchor ("NN / 100" + "trajectory index — not NEWS2") / ordinal ETA bands /
obs-age stamp; P1b partial-NEWS2 comparator; P2 archetype "Pattern:" reframe / scope line / UK
clinical terminology (ghost→hindsight, BREACH→threshold crossed, AEGIS→early warning, ECHO→similar
past trajectories); P3 one Okabe–Ito `styx/viz/palette.py` across all six builders + text-pill ward
badges (no emoji, no state by hue alone). New computation is pure helpers in `styx/readouts.py`
(`styx_index`, `sim_clock`, `eta_ordinal`, partial-NEWS2); copy in `styx/explain.py`.
**Condition relabel — COPD → acute respiratory infection (pneumonia / happy-hypoxia).** The
generator was COPD-*named* but models the opposite phenotype: SpO₂ baselined healthy ~97%, silent
on-air desaturation, effort flat, alert. COPD was never mandated (PRD open question). Docstring/doc
swap only — no generator numbers. This makes NEWS2 **Scale 1** the correct scale and un-blocks the
comparator: on the silent case AEGIS fires 705 min, breach 915 min, **partial NEWS2 first escalates
1010 min** — blind through the whole pre-breach window, crossing ~5 h after AEGIS (Hard-Rule-6
verified in `test_readouts`). **Surface-only proof:** synth diffs are docstring-only; G1–G4 all
green; determinism digest unchanged = `9ea38949db8e5b8c19f969b9919d804013285fb78e0e48f5449c7e76336a5347`.
Full suite 53 passed; ruff clean. Not committed.

**S5.7 notes — clinical-language pass (de-jargon the UI).** *Surface-only; display strings only —
no code identifier renamed, so the digest is bit-identical by construction.* Supersedes 6j's paired
form ("Early warning (AEGIS)") with **plain-only** in all user-facing text; codenames now live solely
in code (keys/modules/docstrings — the engine room). *P1:* one source — `styx/explain.py::DISPLAY_NAMES`
(role→plain UK label, keyed like `COMPONENTS`): aegis→**Early warning**, calliope→**Why this score**,
sentinel→**Confidence**, echo→**Similar past patients**, waterline→**Risk over time**, cone→**Forecast
range**, trajectory→**Trajectory**, ghost→**Hindsight forecast**; caduceus→**How the vital signs move
together** / charon→**Projected care events** *reserved so the reaches ship plain*. Scrubbed the two
codename literals left in the explainer copy (ghost.what "(AEGIS)", echo.why "ECHO illustrates"). *P2:*
both pages + `app.py` + the five viz builders (waterline/cone/timeline/echo/trajectory) + the timeline
`_TECH_LABELS` now render `DISPLAY_NAMES`; removed the 6j "(AEGIS)/(ECHO)" parentheticals; fixed the
**hidden leak** in `01_patient.py` where the clock-tick `name.upper()` rendered "AEGIS" in the slider
(now mapped through `DISPLAY_NAMES`). `STYX` (product) and the 6g Pattern labels kept. *P3 — the
load-bearing guard:* new `tests/test_no_codename.py` (analogue of `test_no_raw_enum`) in **two arms** —
(a) page-text via `AppTest` over both pages, (b) a **figure arm** that builds all five viz figures and
scans `fig.to_dict()` (covers titles/annotations/lane-labels/legend names — invisible to `AppTest`) —
asserting none of {AEGIS, SENTINEL, CALLIOPE, ECHO, CADUCEUS, CHARON} survives (whole-word, case-
insensitive; **no exemptions**, plain-only). Honesty-lint extended (`test_explainer::test_no_codename_in_copy`
over EXPLAINERS⊕TIMELINE_LABELS⊕DISPLAY_NAMES). `test_milestone` F7/F8 presence proxies retargeted off
the codenames to `DISPLAY_NAMES`. **Surface-only proof:** determinism digest re-checked **unchanged =
`9ea38949db8e5b8c19f969b9919d804013285fb78e0e48f5449c7e76336a5347`**; **G1–G4 bit-identical**. Full
suite **58 passed** (53 prior + 5 new); ruff clean. **Not committed.** | **210** (unchanged — no maths
touched).

**S5.8 notes — complete the no-codename guard (step-0 before R1).** *Surface/test-only — no
`styx/` file changed; both gaps closed in the tests that enforce the S5.7 plain-only state.* Two
holes the reaches would have widened: (a) the figure arm scanned a **hardcoded five** builders, so
a new reach builder (R1 hazard panel, R3 graph) would go unscanned — and theograph's `ribbon_figure`
/ `detail_strip_figure` were in fact **already unscanned**; (b) `styx/timeline.py::_TECH_LABELS` (the
S5.7 `aegis`→"Early warning — baseline departure" scrub) sat **outside** the copy-lint, so it or a
sibling could reintroduce a codename ahead of R4 wiring the strip. *P1 — figure arm self-maintains:*
replaced `_patient_figures`/`_ward_figure` with one `_figure_builders()` fixture map keyed by the
builder **function object** → a build-as-the-page-does thunk (seed=42, patient+cohort fit shared); the
scan iterates it. New **meta-test** `test_every_viz_builder_is_scanned` enumerates public `*_figure`
builders in `styx.viz` via `pkgutil`/`inspect` (no `__all__`; the `*_figure` convention + `fn.__module__`
guard are reliable) and asserts each is a fixture-map key — **fails loudly with the builder name** until
a thunk is added. Discovered set (7, all covered, 0 uncovered): `cone_figure`, `echo_figure`,
`ribbon_figure`, `detail_strip_figure`, `timeline_figure`, `trajectory_figure`, `waterline_figure`
(ribbon/detail_strip newly covered). *P2 — enforce `_TECH_LABELS`:* folded into
`test_explainer::test_no_codename_in_copy`; the union is now EXPLAINERS⊕TIMELINE_LABELS⊕DISPLAY_NAMES⊕
**_TECH_LABELS⊕ARCHETYPE_PATTERNS⊕ETA_BANDS** (the remaining display-bound registry dicts). Left out
deliberately (not registry maps, no codename possible): `KMCurve.label` (R1, guard when it lands) and
the computed `sentinel_label`; palette dicts hold colour codes, not copy. **Surface-only proof:**
determinism digest **unchanged = `9ea38949db8e5b8c19f969b9919d804013285fb78e0e48f5449c7e76336a5347`**;
**G1–G4 bit-identical**. Full suite **59 passed** (58 prior + 1 meta-test); ruff clean. **Not committed.**
| **210** (unchanged — no maths touched).
